import React from "react";
import styles from "./index.module.css";

function Footer() {
  return (
    <footer className={styles.outerContainer}>
      <section className={styles.innerContainer}>footer</section>
    </footer>
  );
}

export default Footer;
